# Copy to secrets.yaml and fill in. secrets.yaml must NEVER be committed.
# Nach secrets.yaml kopieren und ausfuellen. secrets.yaml gehoert NIE ins Repo.
wifi_ssid: "MeinWLAN"
wifi_password: "MeinWlanPasswort"
# generate both with / beide generieren mit: openssl rand -base64 32
api_encryption_key: "hier-32-byte-base64"
ota_password: "hier-beliebiges-passwort"
